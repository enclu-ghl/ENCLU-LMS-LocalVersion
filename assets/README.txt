아이콘 등 정적 리소스를 두는 폴더입니다.

icon.ico 를 이 폴더에 넣으면 빌드 시 exe 아이콘으로 자동 적용됩니다
(ENCLU_SCM.spec 이 존재 여부를 확인해서 붙입니다. 없으면 기본 아이콘).

이 README는 폴더 자체를 git에 올리기 위한 자리표시자입니다 —
git은 빈 폴더를 추적하지 않아서, 이 파일이 없으면 CI 체크아웃 시
assets 폴더가 사라집니다.
