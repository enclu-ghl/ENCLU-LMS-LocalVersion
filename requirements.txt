# ENCLU SCM ALL SYSTEM — exe 빌드에 필요한 전체 의존성
#
# 허브 자체(hub/)는 표준 라이브러리만 쓴다. 아래는 exe에 함께 내장되는
# 업무 프로그램들이 쓰는 것들이라, 하나라도 빠지면 그 프로그램만 조용히
# '실행할 수 없습니다'로 뜬다. CI(build.yml)가 이 파일을 그대로 설치한다.

pyinstaller>=6.0

# 공통 — 엑셀 입출력 / DB
pandas
openpyxl
xlsxwriter
xlrd                    # WMS가 내려주는 구형 .xls 읽기
sqlalchemy
psycopg2-binary
python-dotenv

# 파일찢기 (주문파일정리)
beautifulsoup4
lxml                    # ⚠️ BeautifulSoup(html, "lxml") 로 문자열 지정해서 쓴다.
                        #    import 문이 없어 PyInstaller가 못 잡으므로 반드시 여기+spec에 명시.
                        #    빠지면 exe에서 WMS 파일을 읽는 순간 죽는다 (실제 사고).
pykakasi                # 아마존 요미가나 변환

# 박스추천
tkinterdnd2             # 드래그앤드롭 (허브 루트도 이걸로 만든다)
matplotlib              # 3D 시뮬레이션
PyMuPDF                 # fitz — PDF 파싱

# 자동매칭
selenium
